#!/usr/bin/env bash
set -e
if [[ ! -d "/persist/home/neoscode/Development/time-tracker" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/persist/home/neoscode/Development/time-tracker")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/persist/home/neoscode/Development/time-tracker" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/persist/home/neoscode/Development/time-tracker/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/persist/home/neoscode/Development/time-tracker/.envrc" "/persist/home/neoscode/Development/time-tracker/.direnv"/*.rc
